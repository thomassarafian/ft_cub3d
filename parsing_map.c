#include "cub.h"

int	parsing_map(t_cub *cub, char *line)
{
	int i;
	int size_map;

	// size_map = count_map()
	// printf("%d\n", *j);

	i = 0;
	// printf("->%s\n", line);
	// cub->parse.map = malloc(sizeof(char **));
	// if (ft_strchrline == '?')
	// {
	// 	printf("()\n" );
	// 	*cub->parse.map =  *cub->parse.map + ft_strlen(line);
	// }
	// // *cub->parse.map = ft_strdup(line);

	// cub->parse.map[] = ft_strjoin(line, "?");
	// (*cub->parse.map)++;
	// cub->parse.map = ft_split(*cub->parse.map, '?');
	// printf("%s\n", *cub->parse.map);
	// printf("%s\n", line);
	return 1;
}
